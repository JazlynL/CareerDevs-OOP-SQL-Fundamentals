package SQLJOINS;

public class InnerJoin {
    /*
    in the inner join we are only going to get the stuff that matches from
    both tables,basically where there is correlation in the join commands
    * */

    //Select * from Customer inner join City on customer.cityid = city.cityid

}
