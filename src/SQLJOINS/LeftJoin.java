package SQLJOINS;

public class LeftJoin {
    /*When we are using left join we want to get the majority of the information from
    the left table,We want all the matches but we also want the data that doesnt match from the left table
     */
   // Select * from customer left join City on customer.cityId = cityId;
}
